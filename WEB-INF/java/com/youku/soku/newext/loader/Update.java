package com.youku.soku.newext.loader;

import java.text.SimpleDateFormat;
import java.util.Date;
import java.util.Timer;
import java.util.TimerTask;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

public class Update {/*
	private static Log logger=LogFactory.getLog(Update.class);
	final long delay = 60 * 1000;// 60秒
	final long period = 60* 60 * 1000;// 60分钟
	public void start() {
		// 部属数据库加载任务
		SimpleDateFormat sdf=new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
		logger.info("定时更新任务开始: " + sdf.format(new Date()));
		Timer timer = new Timer(true);
		TimerTask updateTask=new UpdateTask();
		timer.schedule(updateTask, delay, period);
		logger.info("定时更新任务结束： "+sdf.format(new Date()));
	}

*/}
