package com.youku.soku.manage.admin.copyright.load;

import java.text.ParseException;
import java.text.SimpleDateFormat;
import java.util.Date;

import org.apache.torque.om.Persistent;

/**
 * 节目配置信息
 * 
 * The skeleton for this class was autogenerated by Torque on:
 * 
 * [Tue Sep 27 12:08:38 CST 2011]
 * 
 * You should add additional methods to this class to meet the application
 * requirements. This class will only be generated as long as it does not
 * already exist in the output directory.
 */
public class ActConfig extends
		com.youku.soku.manage.admin.copyright.load.BaseActConfig implements
		Persistent {
	/** Serial version */
	private static final long serialVersionUID = 1317096518822L;

	public String getCopyrightBegintimeStr() {
		if (null == this.getCopyrightBegintime())
			return "";
		return new SimpleDateFormat("yyyy-MM-dd HH:mm:ss").format(this
				.getCopyrightBegintime());
	}

	public String getCopyrightEndtimeStr() {
		if (null == this.getCopyrightEndtime())
			return "";
		return new SimpleDateFormat("yyyy-MM-dd HH:mm:ss").format(this
				.getCopyrightEndtime());
	}

	public boolean getNeedSpider() {
		if (!this.getIsMonitor())
			return false;
		Date now = new Date();
		Date begin = this.getCopyrightBegintime();
		Date end = this.getCopyrightEndtime();
		if (null == begin) {
			if (null == end)
				return true;
			else
				return end.after(now);
		} else if (null == end)
			return begin.before(now);
		else if (begin.before(now) && end.after(now))
			return true;
		return false;
	}
}
