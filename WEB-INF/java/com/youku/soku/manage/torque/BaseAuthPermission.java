package com.youku.soku.manage.torque;


import java.sql.Connection;
import java.util.ArrayList;
import java.util.Collections;
import java.util.List;

import org.apache.commons.lang.ObjectUtils;
import org.apache.torque.TorqueException;
import org.apache.torque.map.TableMap;
import org.apache.torque.om.BaseObject;
import org.apache.torque.om.NumberKey;
import org.apache.torque.om.ObjectKey;
import org.apache.torque.om.SimpleKey;
import org.apache.torque.util.Criteria;
import org.apache.torque.util.Transaction;





/**
 * Permission Table
 *
 * This class was autogenerated by Torque on:
 *
 * [Wed Mar 10 16:13:12 CST 2010]
 *
 * You should not use this class directly.  It should not even be
 * extended all references should be to AuthPermission
 */
public abstract class BaseAuthPermission extends BaseObject
{
    /** Serial version */
    private static final long serialVersionUID = 1268208792937L;

    /** The Peer class */
    private static final AuthPermissionPeer peer =
        new AuthPermissionPeer();


    /** The value for the id field */
    private int id;

    /** The value for the name field */
    private String name;

    /** The value for the codeName field */
    private String codeName;


    /**
     * Get the Id
     *
     * @return int
     */
    public int getId()
    {
        return id;
    }


    /**
     * Set the value of Id
     *
     * @param v new value
     */
    public void setId(int v) throws TorqueException
    {

        if (this.id != v)
        {
            this.id = v;
            setModified(true);
        }



        // update associated UserPermission
        if (collUserPermissions != null)
        {
            for (int i = 0; i < collUserPermissions.size(); i++)
            {
                ((UserPermission) collUserPermissions.get(i))
                        .setPermissionId(v);
            }
        }
    }

    /**
     * Get the Name
     *
     * @return String
     */
    public String getName()
    {
        return name;
    }


    /**
     * Set the value of Name
     *
     * @param v new value
     */
    public void setName(String v) 
    {

        if (!ObjectUtils.equals(this.name, v))
        {
            this.name = v;
            setModified(true);
        }


    }

    /**
     * Get the CodeName
     *
     * @return String
     */
    public String getCodeName()
    {
        return codeName;
    }


    /**
     * Set the value of CodeName
     *
     * @param v new value
     */
    public void setCodeName(String v) 
    {

        if (!ObjectUtils.equals(this.codeName, v))
        {
            this.codeName = v;
            setModified(true);
        }


    }

       


    /**
     * Collection to store aggregation of collUserPermissions
     */
    protected List collUserPermissions;

    /**
     * Temporary storage of collUserPermissions to save a possible db hit in
     * the event objects are add to the collection, but the
     * complete collection is never requested.
     */
    protected void initUserPermissions()
    {
        if (collUserPermissions == null)
        {
            collUserPermissions = new ArrayList();
        }
    }


    /**
     * Method called to associate a UserPermission object to this object
     * through the UserPermission foreign key attribute
     *
     * @param l UserPermission
     * @throws TorqueException
     */
    public void addUserPermission(UserPermission l) throws TorqueException
    {
        getUserPermissions().add(l);
        l.setAuthPermission((AuthPermission) this);
    }

    /**
     * The criteria used to select the current contents of collUserPermissions
     */
    private Criteria lastUserPermissionsCriteria = null;

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getUserPermissions(new Criteria())
     *
     * @return the collection of associated objects
     * @throws TorqueException
     */
    public List getUserPermissions()
        throws TorqueException
    {
        if (collUserPermissions == null)
        {
            collUserPermissions = getUserPermissions(new Criteria(10));
        }
        return collUserPermissions;
    }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this AuthPermission has previously
     * been saved, it will retrieve related UserPermissions from storage.
     * If this AuthPermission is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     *
     * @throws TorqueException
     */
    public List getUserPermissions(Criteria criteria) throws TorqueException
    {
        if (collUserPermissions == null)
        {
            if (isNew())
            {
               collUserPermissions = new ArrayList();
            }
            else
            {
                criteria.add(UserPermissionPeer.PERMISSION_ID, getId() );
                collUserPermissions = UserPermissionPeer.doSelect(criteria);
            }
        }
        else
        {
            // criteria has no effect for a new object
            if (!isNew())
            {
                // the following code is to determine if a new query is
                // called for.  If the criteria is the same as the last
                // one, just return the collection.
                criteria.add(UserPermissionPeer.PERMISSION_ID, getId());
                if (!lastUserPermissionsCriteria.equals(criteria))
                {
                    collUserPermissions = UserPermissionPeer.doSelect(criteria);
                }
            }
        }
        lastUserPermissionsCriteria = criteria;

        return collUserPermissions;
    }

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getUserPermissions(new Criteria(),Connection)
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getUserPermissions(Connection con) throws TorqueException
    {
        if (collUserPermissions == null)
        {
            collUserPermissions = getUserPermissions(new Criteria(10), con);
        }
        return collUserPermissions;
    }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this AuthPermission has previously
     * been saved, it will retrieve related UserPermissions from storage.
     * If this AuthPermission is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getUserPermissions(Criteria criteria, Connection con)
            throws TorqueException
    {
        if (collUserPermissions == null)
        {
            if (isNew())
            {
               collUserPermissions = new ArrayList();
            }
            else
            {
                 criteria.add(UserPermissionPeer.PERMISSION_ID, getId());
                 collUserPermissions = UserPermissionPeer.doSelect(criteria, con);
             }
         }
         else
         {
             // criteria has no effect for a new object
             if (!isNew())
             {
                 // the following code is to determine if a new query is
                 // called for.  If the criteria is the same as the last
                 // one, just return the collection.
                 criteria.add(UserPermissionPeer.PERMISSION_ID, getId());
                 if (!lastUserPermissionsCriteria.equals(criteria))
                 {
                     collUserPermissions = UserPermissionPeer.doSelect(criteria, con);
                 }
             }
         }
         lastUserPermissionsCriteria = criteria;

         return collUserPermissions;
     }











    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this AuthPermission is new, it will return
     * an empty collection; or if this AuthPermission has previously
     * been saved, it will retrieve related UserPermissions from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in AuthPermission.
     */
    protected List getUserPermissionsJoinUser(Criteria criteria)
        throws TorqueException
    {
        if (collUserPermissions == null)
        {
            if (isNew())
            {
               collUserPermissions = new ArrayList();
            }
            else
            {
                criteria.add(UserPermissionPeer.PERMISSION_ID, getId());
                collUserPermissions = UserPermissionPeer.doSelectJoinUser(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            criteria.add(UserPermissionPeer.PERMISSION_ID, getId());
            if (!lastUserPermissionsCriteria.equals(criteria))
            {
                collUserPermissions = UserPermissionPeer.doSelectJoinUser(criteria);
            }
        }
        lastUserPermissionsCriteria = criteria;

        return collUserPermissions;
    }









    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this AuthPermission is new, it will return
     * an empty collection; or if this AuthPermission has previously
     * been saved, it will retrieve related UserPermissions from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in AuthPermission.
     */
    protected List getUserPermissionsJoinAuthPermission(Criteria criteria)
        throws TorqueException
    {
        if (collUserPermissions == null)
        {
            if (isNew())
            {
               collUserPermissions = new ArrayList();
            }
            else
            {
                criteria.add(UserPermissionPeer.PERMISSION_ID, getId());
                collUserPermissions = UserPermissionPeer.doSelectJoinAuthPermission(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            criteria.add(UserPermissionPeer.PERMISSION_ID, getId());
            if (!lastUserPermissionsCriteria.equals(criteria))
            {
                collUserPermissions = UserPermissionPeer.doSelectJoinAuthPermission(criteria);
            }
        }
        lastUserPermissionsCriteria = criteria;

        return collUserPermissions;
    }



        
    private static List fieldNames = null;

    /**
     * Generate a list of field names.
     *
     * @return a list of field names
     */
    public static synchronized List getFieldNames()
    {
        if (fieldNames == null)
        {
            fieldNames = new ArrayList();
            fieldNames.add("Id");
            fieldNames.add("Name");
            fieldNames.add("CodeName");
            fieldNames = Collections.unmodifiableList(fieldNames);
        }
        return fieldNames;
    }

    /**
     * Retrieves a field from the object by field (Java) name passed in as a String.
     *
     * @param name field name
     * @return value
     */
    public Object getByName(String name)
    {
        if (name.equals("Id"))
        {
            return new Integer(getId());
        }
        if (name.equals("Name"))
        {
            return getName();
        }
        if (name.equals("CodeName"))
        {
            return getCodeName();
        }
        return null;
    }

    /**
     * Set a field in the object by field (Java) name.
     *
     * @param name field name
     * @param value field value
     * @return True if value was set, false if not (invalid name / protected field).
     * @throws IllegalArgumentException if object type of value does not match field object type.
     * @throws TorqueException If a problem occures with the set[Field] method.
     */
    public boolean setByName(String name, Object value )
        throws TorqueException, IllegalArgumentException
    {
        if (name.equals("Id"))
        {
            if (value == null || ! (Integer.class.isInstance(value)))
            {
                throw new IllegalArgumentException("setByName: value parameter was null or not an Integer object.");
            }
            setId(((Integer) value).intValue());
            return true;
        }
        if (name.equals("Name"))
        {
            // Object fields can be null
            if (value != null && ! String.class.isInstance(value))
            {
                throw new IllegalArgumentException("Invalid type of object specified for value in setByName");
            }
            setName((String) value);
            return true;
        }
        if (name.equals("CodeName"))
        {
            // Object fields can be null
            if (value != null && ! String.class.isInstance(value))
            {
                throw new IllegalArgumentException("Invalid type of object specified for value in setByName");
            }
            setCodeName((String) value);
            return true;
        }
        return false;
    }

    /**
     * Retrieves a field from the object by name passed in
     * as a String.  The String must be one of the static
     * Strings defined in this Class' Peer.
     *
     * @param name peer name
     * @return value
     */
    public Object getByPeerName(String name)
    {
        if (name.equals(AuthPermissionPeer.ID))
        {
            return new Integer(getId());
        }
        if (name.equals(AuthPermissionPeer.NAME))
        {
            return getName();
        }
        if (name.equals(AuthPermissionPeer.CODE_NAME))
        {
            return getCodeName();
        }
        return null;
    }

    /**
     * Set field values by Peer Field Name
     *
     * @param name field name
     * @param value field value
     * @return True if value was set, false if not (invalid name / protected field).
     * @throws IllegalArgumentException if object type of value does not match field object type.
     * @throws TorqueException If a problem occures with the set[Field] method.
     */
    public boolean setByPeerName(String name, Object value)
        throws TorqueException, IllegalArgumentException
    {
      if (AuthPermissionPeer.ID.equals(name))
        {
            return setByName("Id", value);
        }
      if (AuthPermissionPeer.NAME.equals(name))
        {
            return setByName("Name", value);
        }
      if (AuthPermissionPeer.CODE_NAME.equals(name))
        {
            return setByName("CodeName", value);
        }
        return false;
    }

    /**
     * Retrieves a field from the object by Position as specified
     * in the xml schema.  Zero-based.
     *
     * @param pos position in xml schema
     * @return value
     */
    public Object getByPosition(int pos)
    {
        if (pos == 0)
        {
            return new Integer(getId());
        }
        if (pos == 1)
        {
            return getName();
        }
        if (pos == 2)
        {
            return getCodeName();
        }
        return null;
    }

    /**
     * Set field values by its position (zero based) in the XML schema.
     *
     * @param position The field position
     * @param value field value
     * @return True if value was set, false if not (invalid position / protected field).
     * @throws IllegalArgumentException if object type of value does not match field object type.
     * @throws TorqueException If a problem occures with the set[Field] method.
     */
    public boolean setByPosition(int position, Object value)
        throws TorqueException, IllegalArgumentException
    {
    if (position == 0)
        {
            return setByName("Id", value);
        }
    if (position == 1)
        {
            return setByName("Name", value);
        }
    if (position == 2)
        {
            return setByName("CodeName", value);
        }
        return false;
    }
     
    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
     *
     * @throws Exception
     */
    public void save() throws Exception
    {
        save(AuthPermissionPeer.DATABASE_NAME);
    }

    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
     * Note: this code is here because the method body is
     * auto-generated conditionally and therefore needs to be
     * in this file instead of in the super class, BaseObject.
     *
     * @param dbName
     * @throws TorqueException
     */
    public void save(String dbName) throws TorqueException
    {
        Connection con = null;
        try
        {
            con = Transaction.begin(dbName);
            save(con);
            Transaction.commit(con);
        }
        catch(TorqueException e)
        {
            Transaction.safeRollback(con);
            throw e;
        }
    }

    /** flag to prevent endless save loop, if this object is referenced
        by another object which falls in this transaction. */
    private boolean alreadyInSave = false;
    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.  This method
     * is meant to be used as part of a transaction, otherwise use
     * the save() method and the connection details will be handled
     * internally
     *
     * @param con
     * @throws TorqueException
     */
    public void save(Connection con) throws TorqueException
    {
        if (!alreadyInSave)
        {
            alreadyInSave = true;



            // If this object has been modified, then save it to the database.
            if (isModified())
            {
                if (isNew())
                {
                    AuthPermissionPeer.doInsert((AuthPermission) this, con);
                    setNew(false);
                }
                else
                {
                    AuthPermissionPeer.doUpdate((AuthPermission) this, con);
                }
            }


            if (collUserPermissions != null)
            {
                for (int i = 0; i < collUserPermissions.size(); i++)
                {
                    ((UserPermission) collUserPermissions.get(i)).save(con);
                }
            }
            alreadyInSave = false;
        }
    }


    /**
     * Set the PrimaryKey using ObjectKey.
     *
     * @param key id ObjectKey
     */
    public void setPrimaryKey(ObjectKey key)
        throws TorqueException
    {
        setId(((NumberKey) key).intValue());
    }

    /**
     * Set the PrimaryKey using a String.
     *
     * @param key
     */
    public void setPrimaryKey(String key) throws TorqueException
    {
        setId(Integer.parseInt(key));
    }


    /**
     * returns an id that differentiates this object from others
     * of its class.
     */
    public ObjectKey getPrimaryKey()
    {
        return SimpleKey.keyFor(getId());
    }
 

    /**
     * Makes a copy of this object.
     * It creates a new object filling in the simple attributes.
     * It then fills all the association collections and sets the
     * related objects to isNew=true.
     */
    public AuthPermission copy() throws TorqueException
    {
        return copy(true);
    }

    /**
     * Makes a copy of this object.
     * It creates a new object filling in the simple attributes.
     * If the parameter deepcopy is true, it then fills all the
     * association collections and sets the related objects to
     * isNew=true.
     *
     * @param deepcopy whether to copy the associated objects.
     */
    public AuthPermission copy(boolean deepcopy) throws TorqueException
    {
        return copyInto(new AuthPermission(), deepcopy);
    }

    /**
     * Fills the copyObj with the contents of this object.
     * The associated objects are also copied and treated as new objects.
     * @param copyObj the object to fill.
     */
    protected AuthPermission copyInto(AuthPermission copyObj) throws TorqueException
    {
        return copyInto(copyObj, true);
    }

    /**
     * Fills the copyObj with the contents of this object.
     * If deepcopy is true, The associated objects are also copied
     * and treated as new objects.
     * @param copyObj the object to fill.
     * @param deepcopy whether the associated objects should be copied.
     */
    protected AuthPermission copyInto(AuthPermission copyObj, boolean deepcopy) throws TorqueException
    {
        copyObj.setId(id);
        copyObj.setName(name);
        copyObj.setCodeName(codeName);

        copyObj.setId( 0);

        if (deepcopy)
        {


        List vUserPermissions = getUserPermissions();
        if (vUserPermissions != null)
        {
            for (int i = 0; i < vUserPermissions.size(); i++)
            {
                UserPermission obj = (UserPermission) vUserPermissions.get(i);
                copyObj.addUserPermission(obj.copy());
            }
        }
        else
        {
            copyObj.collUserPermissions = null;
        }
        }
        return copyObj;
    }

    /**
     * returns a peer instance associated with this om.  Since Peer classes
     * are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     */
    public AuthPermissionPeer getPeer()
    {
        return peer;
    }

    /**
     * Retrieves the TableMap object related to this Table data without
     * compiler warnings of using getPeer().getTableMap().
     *
     * @return The associated TableMap object.
     */
    public TableMap getTableMap() throws TorqueException
    {
        return AuthPermissionPeer.getTableMap();
    }


    public String toString()
    {
        StringBuffer str = new StringBuffer();
        str.append("AuthPermission:\n");
        str.append("Id = ")
           .append(getId())
           .append("\n");
        str.append("Name = ")
           .append(getName())
           .append("\n");
        str.append("CodeName = ")
           .append(getCodeName())
           .append("\n");
        return(str.toString());
    }
}
