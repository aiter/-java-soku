package com.youku.soku.manage.torque;


import java.sql.Connection;
import java.util.ArrayList;
import java.util.Collections;
import java.util.List;

import org.apache.commons.lang.ObjectUtils;
import org.apache.torque.TorqueException;
import org.apache.torque.map.TableMap;
import org.apache.torque.om.BaseObject;
import org.apache.torque.om.NumberKey;
import org.apache.torque.om.ObjectKey;
import org.apache.torque.om.SimpleKey;
import org.apache.torque.util.Criteria;
import org.apache.torque.util.Transaction;





/**
 * keyword to interven
 *
 * This class was autogenerated by Torque on:
 *
 * [Wed Mar 10 16:13:12 CST 2010]
 *
 * You should not use this class directly.  It should not even be
 * extended all references should be to KeywordInterven
 */
public abstract class BaseKeywordInterven extends BaseObject
{
    /** Serial version */
    private static final long serialVersionUID = 1268208792937L;

    /** The Peer class */
    private static final KeywordIntervenPeer peer =
        new KeywordIntervenPeer();


    /** The value for the keywordId field */
    private int keywordId;

    /** The value for the name field */
    private String name;


    /**
     * Get the KeywordId
     *
     * @return int
     */
    public int getKeywordId()
    {
        return keywordId;
    }


    /**
     * Set the value of KeywordId
     *
     * @param v new value
     */
    public void setKeywordId(int v) throws TorqueException
    {

        if (this.keywordId != v)
        {
            this.keywordId = v;
            setModified(true);
        }



        // update associated KeywordIntervenVideo
        if (collKeywordIntervenVideos != null)
        {
            for (int i = 0; i < collKeywordIntervenVideos.size(); i++)
            {
                ((KeywordIntervenVideo) collKeywordIntervenVideos.get(i))
                        .setKeywordId(v);
            }
        }
    }

    /**
     * Get the Name
     *
     * @return String
     */
    public String getName()
    {
        return name;
    }


    /**
     * Set the value of Name
     *
     * @param v new value
     */
    public void setName(String v) 
    {

        if (!ObjectUtils.equals(this.name, v))
        {
            this.name = v;
            setModified(true);
        }


    }

       


    /**
     * Collection to store aggregation of collKeywordIntervenVideos
     */
    protected List collKeywordIntervenVideos;

    /**
     * Temporary storage of collKeywordIntervenVideos to save a possible db hit in
     * the event objects are add to the collection, but the
     * complete collection is never requested.
     */
    protected void initKeywordIntervenVideos()
    {
        if (collKeywordIntervenVideos == null)
        {
            collKeywordIntervenVideos = new ArrayList();
        }
    }


    /**
     * Method called to associate a KeywordIntervenVideo object to this object
     * through the KeywordIntervenVideo foreign key attribute
     *
     * @param l KeywordIntervenVideo
     * @throws TorqueException
     */
    public void addKeywordIntervenVideo(KeywordIntervenVideo l) throws TorqueException
    {
        getKeywordIntervenVideos().add(l);
        l.setKeywordInterven((KeywordInterven) this);
    }

    /**
     * The criteria used to select the current contents of collKeywordIntervenVideos
     */
    private Criteria lastKeywordIntervenVideosCriteria = null;

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getKeywordIntervenVideos(new Criteria())
     *
     * @return the collection of associated objects
     * @throws TorqueException
     */
    public List getKeywordIntervenVideos()
        throws TorqueException
    {
        if (collKeywordIntervenVideos == null)
        {
            collKeywordIntervenVideos = getKeywordIntervenVideos(new Criteria(10));
        }
        return collKeywordIntervenVideos;
    }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this KeywordInterven has previously
     * been saved, it will retrieve related KeywordIntervenVideos from storage.
     * If this KeywordInterven is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     *
     * @throws TorqueException
     */
    public List getKeywordIntervenVideos(Criteria criteria) throws TorqueException
    {
        if (collKeywordIntervenVideos == null)
        {
            if (isNew())
            {
               collKeywordIntervenVideos = new ArrayList();
            }
            else
            {
                criteria.add(KeywordIntervenVideoPeer.KEYWORD_ID, getKeywordId() );
                collKeywordIntervenVideos = KeywordIntervenVideoPeer.doSelect(criteria);
            }
        }
        else
        {
            // criteria has no effect for a new object
            if (!isNew())
            {
                // the following code is to determine if a new query is
                // called for.  If the criteria is the same as the last
                // one, just return the collection.
                criteria.add(KeywordIntervenVideoPeer.KEYWORD_ID, getKeywordId());
                if (!lastKeywordIntervenVideosCriteria.equals(criteria))
                {
                    collKeywordIntervenVideos = KeywordIntervenVideoPeer.doSelect(criteria);
                }
            }
        }
        lastKeywordIntervenVideosCriteria = criteria;

        return collKeywordIntervenVideos;
    }

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getKeywordIntervenVideos(new Criteria(),Connection)
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getKeywordIntervenVideos(Connection con) throws TorqueException
    {
        if (collKeywordIntervenVideos == null)
        {
            collKeywordIntervenVideos = getKeywordIntervenVideos(new Criteria(10), con);
        }
        return collKeywordIntervenVideos;
    }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this KeywordInterven has previously
     * been saved, it will retrieve related KeywordIntervenVideos from storage.
     * If this KeywordInterven is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getKeywordIntervenVideos(Criteria criteria, Connection con)
            throws TorqueException
    {
        if (collKeywordIntervenVideos == null)
        {
            if (isNew())
            {
               collKeywordIntervenVideos = new ArrayList();
            }
            else
            {
                 criteria.add(KeywordIntervenVideoPeer.KEYWORD_ID, getKeywordId());
                 collKeywordIntervenVideos = KeywordIntervenVideoPeer.doSelect(criteria, con);
             }
         }
         else
         {
             // criteria has no effect for a new object
             if (!isNew())
             {
                 // the following code is to determine if a new query is
                 // called for.  If the criteria is the same as the last
                 // one, just return the collection.
                 criteria.add(KeywordIntervenVideoPeer.KEYWORD_ID, getKeywordId());
                 if (!lastKeywordIntervenVideosCriteria.equals(criteria))
                 {
                     collKeywordIntervenVideos = KeywordIntervenVideoPeer.doSelect(criteria, con);
                 }
             }
         }
         lastKeywordIntervenVideosCriteria = criteria;

         return collKeywordIntervenVideos;
     }











    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this KeywordInterven is new, it will return
     * an empty collection; or if this KeywordInterven has previously
     * been saved, it will retrieve related KeywordIntervenVideos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in KeywordInterven.
     */
    protected List getKeywordIntervenVideosJoinKeywordInterven(Criteria criteria)
        throws TorqueException
    {
        if (collKeywordIntervenVideos == null)
        {
            if (isNew())
            {
               collKeywordIntervenVideos = new ArrayList();
            }
            else
            {
                criteria.add(KeywordIntervenVideoPeer.KEYWORD_ID, getKeywordId());
                collKeywordIntervenVideos = KeywordIntervenVideoPeer.doSelectJoinKeywordInterven(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            criteria.add(KeywordIntervenVideoPeer.KEYWORD_ID, getKeywordId());
            if (!lastKeywordIntervenVideosCriteria.equals(criteria))
            {
                collKeywordIntervenVideos = KeywordIntervenVideoPeer.doSelectJoinKeywordInterven(criteria);
            }
        }
        lastKeywordIntervenVideosCriteria = criteria;

        return collKeywordIntervenVideos;
    }



        
    private static List fieldNames = null;

    /**
     * Generate a list of field names.
     *
     * @return a list of field names
     */
    public static synchronized List getFieldNames()
    {
        if (fieldNames == null)
        {
            fieldNames = new ArrayList();
            fieldNames.add("KeywordId");
            fieldNames.add("Name");
            fieldNames = Collections.unmodifiableList(fieldNames);
        }
        return fieldNames;
    }

    /**
     * Retrieves a field from the object by field (Java) name passed in as a String.
     *
     * @param name field name
     * @return value
     */
    public Object getByName(String name)
    {
        if (name.equals("KeywordId"))
        {
            return new Integer(getKeywordId());
        }
        if (name.equals("Name"))
        {
            return getName();
        }
        return null;
    }

    /**
     * Set a field in the object by field (Java) name.
     *
     * @param name field name
     * @param value field value
     * @return True if value was set, false if not (invalid name / protected field).
     * @throws IllegalArgumentException if object type of value does not match field object type.
     * @throws TorqueException If a problem occures with the set[Field] method.
     */
    public boolean setByName(String name, Object value )
        throws TorqueException, IllegalArgumentException
    {
        if (name.equals("KeywordId"))
        {
            if (value == null || ! (Integer.class.isInstance(value)))
            {
                throw new IllegalArgumentException("setByName: value parameter was null or not an Integer object.");
            }
            setKeywordId(((Integer) value).intValue());
            return true;
        }
        if (name.equals("Name"))
        {
            // Object fields can be null
            if (value != null && ! String.class.isInstance(value))
            {
                throw new IllegalArgumentException("Invalid type of object specified for value in setByName");
            }
            setName((String) value);
            return true;
        }
        return false;
    }

    /**
     * Retrieves a field from the object by name passed in
     * as a String.  The String must be one of the static
     * Strings defined in this Class' Peer.
     *
     * @param name peer name
     * @return value
     */
    public Object getByPeerName(String name)
    {
        if (name.equals(KeywordIntervenPeer.KEYWORD_ID))
        {
            return new Integer(getKeywordId());
        }
        if (name.equals(KeywordIntervenPeer.NAME))
        {
            return getName();
        }
        return null;
    }

    /**
     * Set field values by Peer Field Name
     *
     * @param name field name
     * @param value field value
     * @return True if value was set, false if not (invalid name / protected field).
     * @throws IllegalArgumentException if object type of value does not match field object type.
     * @throws TorqueException If a problem occures with the set[Field] method.
     */
    public boolean setByPeerName(String name, Object value)
        throws TorqueException, IllegalArgumentException
    {
      if (KeywordIntervenPeer.KEYWORD_ID.equals(name))
        {
            return setByName("KeywordId", value);
        }
      if (KeywordIntervenPeer.NAME.equals(name))
        {
            return setByName("Name", value);
        }
        return false;
    }

    /**
     * Retrieves a field from the object by Position as specified
     * in the xml schema.  Zero-based.
     *
     * @param pos position in xml schema
     * @return value
     */
    public Object getByPosition(int pos)
    {
        if (pos == 0)
        {
            return new Integer(getKeywordId());
        }
        if (pos == 1)
        {
            return getName();
        }
        return null;
    }

    /**
     * Set field values by its position (zero based) in the XML schema.
     *
     * @param position The field position
     * @param value field value
     * @return True if value was set, false if not (invalid position / protected field).
     * @throws IllegalArgumentException if object type of value does not match field object type.
     * @throws TorqueException If a problem occures with the set[Field] method.
     */
    public boolean setByPosition(int position, Object value)
        throws TorqueException, IllegalArgumentException
    {
    if (position == 0)
        {
            return setByName("KeywordId", value);
        }
    if (position == 1)
        {
            return setByName("Name", value);
        }
        return false;
    }
     
    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
     *
     * @throws Exception
     */
    public void save() throws Exception
    {
        save(KeywordIntervenPeer.DATABASE_NAME);
    }

    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
     * Note: this code is here because the method body is
     * auto-generated conditionally and therefore needs to be
     * in this file instead of in the super class, BaseObject.
     *
     * @param dbName
     * @throws TorqueException
     */
    public void save(String dbName) throws TorqueException
    {
        Connection con = null;
        try
        {
            con = Transaction.begin(dbName);
            save(con);
            Transaction.commit(con);
        }
        catch(TorqueException e)
        {
            Transaction.safeRollback(con);
            throw e;
        }
    }

    /** flag to prevent endless save loop, if this object is referenced
        by another object which falls in this transaction. */
    private boolean alreadyInSave = false;
    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.  This method
     * is meant to be used as part of a transaction, otherwise use
     * the save() method and the connection details will be handled
     * internally
     *
     * @param con
     * @throws TorqueException
     */
    public void save(Connection con) throws TorqueException
    {
        if (!alreadyInSave)
        {
            alreadyInSave = true;



            // If this object has been modified, then save it to the database.
            if (isModified())
            {
                if (isNew())
                {
                    KeywordIntervenPeer.doInsert((KeywordInterven) this, con);
                    setNew(false);
                }
                else
                {
                    KeywordIntervenPeer.doUpdate((KeywordInterven) this, con);
                }
            }


            if (collKeywordIntervenVideos != null)
            {
                for (int i = 0; i < collKeywordIntervenVideos.size(); i++)
                {
                    ((KeywordIntervenVideo) collKeywordIntervenVideos.get(i)).save(con);
                }
            }
            alreadyInSave = false;
        }
    }


    /**
     * Set the PrimaryKey using ObjectKey.
     *
     * @param key keywordId ObjectKey
     */
    public void setPrimaryKey(ObjectKey key)
        throws TorqueException
    {
        setKeywordId(((NumberKey) key).intValue());
    }

    /**
     * Set the PrimaryKey using a String.
     *
     * @param key
     */
    public void setPrimaryKey(String key) throws TorqueException
    {
        setKeywordId(Integer.parseInt(key));
    }


    /**
     * returns an id that differentiates this object from others
     * of its class.
     */
    public ObjectKey getPrimaryKey()
    {
        return SimpleKey.keyFor(getKeywordId());
    }
 

    /**
     * Makes a copy of this object.
     * It creates a new object filling in the simple attributes.
     * It then fills all the association collections and sets the
     * related objects to isNew=true.
     */
    public KeywordInterven copy() throws TorqueException
    {
        return copy(true);
    }

    /**
     * Makes a copy of this object.
     * It creates a new object filling in the simple attributes.
     * If the parameter deepcopy is true, it then fills all the
     * association collections and sets the related objects to
     * isNew=true.
     *
     * @param deepcopy whether to copy the associated objects.
     */
    public KeywordInterven copy(boolean deepcopy) throws TorqueException
    {
        return copyInto(new KeywordInterven(), deepcopy);
    }

    /**
     * Fills the copyObj with the contents of this object.
     * The associated objects are also copied and treated as new objects.
     * @param copyObj the object to fill.
     */
    protected KeywordInterven copyInto(KeywordInterven copyObj) throws TorqueException
    {
        return copyInto(copyObj, true);
    }

    /**
     * Fills the copyObj with the contents of this object.
     * If deepcopy is true, The associated objects are also copied
     * and treated as new objects.
     * @param copyObj the object to fill.
     * @param deepcopy whether the associated objects should be copied.
     */
    protected KeywordInterven copyInto(KeywordInterven copyObj, boolean deepcopy) throws TorqueException
    {
        copyObj.setKeywordId(keywordId);
        copyObj.setName(name);

        copyObj.setKeywordId( 0);

        if (deepcopy)
        {


        List vKeywordIntervenVideos = getKeywordIntervenVideos();
        if (vKeywordIntervenVideos != null)
        {
            for (int i = 0; i < vKeywordIntervenVideos.size(); i++)
            {
                KeywordIntervenVideo obj = (KeywordIntervenVideo) vKeywordIntervenVideos.get(i);
                copyObj.addKeywordIntervenVideo(obj.copy());
            }
        }
        else
        {
            copyObj.collKeywordIntervenVideos = null;
        }
        }
        return copyObj;
    }

    /**
     * returns a peer instance associated with this om.  Since Peer classes
     * are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     */
    public KeywordIntervenPeer getPeer()
    {
        return peer;
    }

    /**
     * Retrieves the TableMap object related to this Table data without
     * compiler warnings of using getPeer().getTableMap().
     *
     * @return The associated TableMap object.
     */
    public TableMap getTableMap() throws TorqueException
    {
        return KeywordIntervenPeer.getTableMap();
    }


    public String toString()
    {
        StringBuffer str = new StringBuffer();
        str.append("KeywordInterven:\n");
        str.append("KeywordId = ")
           .append(getKeywordId())
           .append("\n");
        str.append("Name = ")
           .append(getName())
           .append("\n");
        return(str.toString());
    }
}
