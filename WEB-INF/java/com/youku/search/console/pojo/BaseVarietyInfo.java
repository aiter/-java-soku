package com.youku.search.console.pojo;


import java.math.BigDecimal;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.Collections;
import java.util.Date;
import java.util.List;

import org.apache.commons.lang.ObjectUtils;
import org.apache.torque.TorqueException;
import org.apache.torque.map.TableMap;
import org.apache.torque.om.BaseObject;
import org.apache.torque.om.ComboKey;
import org.apache.torque.om.DateKey;
import org.apache.torque.om.NumberKey;
import org.apache.torque.om.ObjectKey;
import org.apache.torque.om.SimpleKey;
import org.apache.torque.om.StringKey;
import org.apache.torque.om.Persistent;
import org.apache.torque.util.Criteria;
import org.apache.torque.util.Transaction;



            

/**
 * ¡Á???????????????
 *
 * This class was autogenerated by Torque on:
 *
 * [Tue Feb 10 15:25:22 CST 2009]
 *
 * You should not use this class directly.  It should not even be
 * extended all references should be to VarietyInfo
 */
public abstract class BaseVarietyInfo extends BaseObject
{
    /** Serial version */
    private static final long serialVersionUID = 1234250722796L;

    /** The Peer class */
    private static final VarietyInfoPeer peer =
        new VarietyInfoPeer();

        
    /** The value for the id field */
    private int id;
      
    /** The value for the fkVarietyId field */
    private int fkVarietyId;
      
    /** The value for the episodeCount field */
    private int episodeCount;
      
    /** The value for the totalCount field */
    private int totalCount;
                                          
    /** The value for the fixed field */
    private int fixed = 0;
      
    /** The value for the firstlogo field */
    private String firstlogo;
  
            
    /**
     * Get the Id
     *
     * @return int
     */
    public int getId()
    {
        return id;
    }

                        
    /**
     * Set the value of Id
     *
     * @param v new value
     */
    public void setId(int v) 
    {
    
                  if (this.id != v)
              {
            this.id = v;
            setModified(true);
        }
    
          
              }
          
    /**
     * Get the FkVarietyId
     *
     * @return int
     */
    public int getFkVarietyId()
    {
        return fkVarietyId;
    }

                        
    /**
     * Set the value of FkVarietyId
     *
     * @param v new value
     */
    public void setFkVarietyId(int v) 
    {
    
                  if (this.fkVarietyId != v)
              {
            this.fkVarietyId = v;
            setModified(true);
        }
    
          
              }
          
    /**
     * Get the EpisodeCount
     *
     * @return int
     */
    public int getEpisodeCount()
    {
        return episodeCount;
    }

                        
    /**
     * Set the value of EpisodeCount
     *
     * @param v new value
     */
    public void setEpisodeCount(int v) 
    {
    
                  if (this.episodeCount != v)
              {
            this.episodeCount = v;
            setModified(true);
        }
    
          
              }
          
    /**
     * Get the TotalCount
     *
     * @return int
     */
    public int getTotalCount()
    {
        return totalCount;
    }

                        
    /**
     * Set the value of TotalCount
     *
     * @param v new value
     */
    public void setTotalCount(int v) 
    {
    
                  if (this.totalCount != v)
              {
            this.totalCount = v;
            setModified(true);
        }
    
          
              }
          
    /**
     * Get the Fixed
     *
     * @return int
     */
    public int getFixed()
    {
        return fixed;
    }

                        
    /**
     * Set the value of Fixed
     *
     * @param v new value
     */
    public void setFixed(int v) 
    {
    
                  if (this.fixed != v)
              {
            this.fixed = v;
            setModified(true);
        }
    
          
              }
          
    /**
     * Get the Firstlogo
     *
     * @return String
     */
    public String getFirstlogo()
    {
        return firstlogo;
    }

                        
    /**
     * Set the value of Firstlogo
     *
     * @param v new value
     */
    public void setFirstlogo(String v) 
    {
    
                  if (!ObjectUtils.equals(this.firstlogo, v))
              {
            this.firstlogo = v;
            setModified(true);
        }
    
          
              }
  
         
                
    private static List<String> fieldNames = null;

    /**
     * Generate a list of field names.
     *
     * @return a list of field names
     */
    public static synchronized List<String> getFieldNames()
    {
        if (fieldNames == null)
        {
            fieldNames = new ArrayList<String>();
              fieldNames.add("Id");
              fieldNames.add("FkVarietyId");
              fieldNames.add("EpisodeCount");
              fieldNames.add("TotalCount");
              fieldNames.add("Fixed");
              fieldNames.add("Firstlogo");
              fieldNames = Collections.unmodifiableList(fieldNames);
        }
        return fieldNames;
    }

    /**
     * Retrieves a field from the object by field (Java) name passed in as a String.
     *
     * @param name field name
     * @return value
     */
    public Object getByName(String name)
    {
          if (name.equals("Id"))
        {
                return new Integer(getId());
            }
          if (name.equals("FkVarietyId"))
        {
                return new Integer(getFkVarietyId());
            }
          if (name.equals("EpisodeCount"))
        {
                return new Integer(getEpisodeCount());
            }
          if (name.equals("TotalCount"))
        {
                return new Integer(getTotalCount());
            }
          if (name.equals("Fixed"))
        {
                return new Integer(getFixed());
            }
          if (name.equals("Firstlogo"))
        {
                return getFirstlogo();
            }
          return null;
    }

    /**
     * Set a field in the object by field (Java) name.
     *
     * @param name field name
     * @param value field value
     * @return True if value was set, false if not (invalid name / protected field).
     * @throws IllegalArgumentException if object type of value does not match field object type.
     * @throws TorqueException If a problem occures with the set[Field] method.
     */
    public boolean setByName(String name, Object value )
        throws TorqueException, IllegalArgumentException
    {
          if (name.equals("Id"))
        {
                      if (value == null || ! (Integer.class.isInstance(value)))
            {
                throw new IllegalArgumentException("setByName: value parameter was null or not an Integer object.");
            }
            setId(((Integer) value).intValue());
                      return true;
        }
          if (name.equals("FkVarietyId"))
        {
                      if (value == null || ! (Integer.class.isInstance(value)))
            {
                throw new IllegalArgumentException("setByName: value parameter was null or not an Integer object.");
            }
            setFkVarietyId(((Integer) value).intValue());
                      return true;
        }
          if (name.equals("EpisodeCount"))
        {
                      if (value == null || ! (Integer.class.isInstance(value)))
            {
                throw new IllegalArgumentException("setByName: value parameter was null or not an Integer object.");
            }
            setEpisodeCount(((Integer) value).intValue());
                      return true;
        }
          if (name.equals("TotalCount"))
        {
                      if (value == null || ! (Integer.class.isInstance(value)))
            {
                throw new IllegalArgumentException("setByName: value parameter was null or not an Integer object.");
            }
            setTotalCount(((Integer) value).intValue());
                      return true;
        }
          if (name.equals("Fixed"))
        {
                      if (value == null || ! (Integer.class.isInstance(value)))
            {
                throw new IllegalArgumentException("setByName: value parameter was null or not an Integer object.");
            }
            setFixed(((Integer) value).intValue());
                      return true;
        }
          if (name.equals("Firstlogo"))
        {
                      // Object fields can be null
            if (value != null && ! String.class.isInstance(value))
            {
                throw new IllegalArgumentException("Invalid type of object specified for value in setByName");
            }
            setFirstlogo((String) value);
                      return true;
        }
          return false;
    }

    /**
     * Retrieves a field from the object by name passed in
     * as a String.  The String must be one of the static
     * Strings defined in this Class' Peer.
     *
     * @param name peer name
     * @return value
     */
    public Object getByPeerName(String name)
    {
          if (name.equals(VarietyInfoPeer.ID))
        {
                return new Integer(getId());
            }
          if (name.equals(VarietyInfoPeer.FK_VARIETY_ID))
        {
                return new Integer(getFkVarietyId());
            }
          if (name.equals(VarietyInfoPeer.EPISODE_COUNT))
        {
                return new Integer(getEpisodeCount());
            }
          if (name.equals(VarietyInfoPeer.TOTAL_COUNT))
        {
                return new Integer(getTotalCount());
            }
          if (name.equals(VarietyInfoPeer.FIXED))
        {
                return new Integer(getFixed());
            }
          if (name.equals(VarietyInfoPeer.FIRSTLOGO))
        {
                return getFirstlogo();
            }
          return null;
    }

    /**
     * Set field values by Peer Field Name
     *
     * @param name field name
     * @param value field value
     * @return True if value was set, false if not (invalid name / protected field).
     * @throws IllegalArgumentException if object type of value does not match field object type.
     * @throws TorqueException If a problem occures with the set[Field] method.
     */
    public boolean setByPeerName(String name, Object value)
        throws TorqueException, IllegalArgumentException
    {
        if (VarietyInfoPeer.ID.equals(name))
        {
            return setByName("Id", value);
        }
        if (VarietyInfoPeer.FK_VARIETY_ID.equals(name))
        {
            return setByName("FkVarietyId", value);
        }
        if (VarietyInfoPeer.EPISODE_COUNT.equals(name))
        {
            return setByName("EpisodeCount", value);
        }
        if (VarietyInfoPeer.TOTAL_COUNT.equals(name))
        {
            return setByName("TotalCount", value);
        }
        if (VarietyInfoPeer.FIXED.equals(name))
        {
            return setByName("Fixed", value);
        }
        if (VarietyInfoPeer.FIRSTLOGO.equals(name))
        {
            return setByName("Firstlogo", value);
        }
          return false;
    }

    /**
     * Retrieves a field from the object by Position as specified
     * in the xml schema.  Zero-based.
     *
     * @param pos position in xml schema
     * @return value
     */
    public Object getByPosition(int pos)
    {
            if (pos == 0)
        {
                return new Integer(getId());
            }
              if (pos == 1)
        {
                return new Integer(getFkVarietyId());
            }
              if (pos == 2)
        {
                return new Integer(getEpisodeCount());
            }
              if (pos == 3)
        {
                return new Integer(getTotalCount());
            }
              if (pos == 4)
        {
                return new Integer(getFixed());
            }
              if (pos == 5)
        {
                return getFirstlogo();
            }
              return null;
    }

    /**
     * Set field values by its position (zero based) in the XML schema.
     *
     * @param position The field position
     * @param value field value
     * @return True if value was set, false if not (invalid position / protected field).
     * @throws IllegalArgumentException if object type of value does not match field object type.
     * @throws TorqueException If a problem occures with the set[Field] method.
     */
    public boolean setByPosition(int position, Object value)
        throws TorqueException, IllegalArgumentException
    {
        if (position == 0)
        {
            return setByName("Id", value);
        }
          if (position == 1)
        {
            return setByName("FkVarietyId", value);
        }
          if (position == 2)
        {
            return setByName("EpisodeCount", value);
        }
          if (position == 3)
        {
            return setByName("TotalCount", value);
        }
          if (position == 4)
        {
            return setByName("Fixed", value);
        }
          if (position == 5)
        {
            return setByName("Firstlogo", value);
        }
              return false;
    }
     
    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
     *
     * @throws Exception
     */
    public void save() throws Exception
    {
          save(VarietyInfoPeer.DATABASE_NAME);
      }

    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
       * Note: this code is here because the method body is
     * auto-generated conditionally and therefore needs to be
     * in this file instead of in the super class, BaseObject.
       *
     * @param dbName
     * @throws TorqueException
     */
    public void save(String dbName) throws TorqueException
    {
        Connection con = null;
          try
        {
            con = Transaction.begin(dbName);
            save(con);
            Transaction.commit(con);
        }
        catch(TorqueException e)
        {
            Transaction.safeRollback(con);
            throw e;
        }
      }

      /** flag to prevent endless save loop, if this object is referenced
        by another object which falls in this transaction. */
    private boolean alreadyInSave = false;
      /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.  This method
     * is meant to be used as part of a transaction, otherwise use
     * the save() method and the connection details will be handled
     * internally
     *
     * @param con
     * @throws TorqueException
     */
    public void save(Connection con) throws TorqueException
    {
          if (!alreadyInSave)
        {
            alreadyInSave = true;


  
            // If this object has been modified, then save it to the database.
            if (isModified())
            {
                if (isNew())
                {
                    VarietyInfoPeer.doInsert((VarietyInfo) this, con);
                    setNew(false);
                }
                else
                {
                    VarietyInfoPeer.doUpdate((VarietyInfo) this, con);
                }
                }

                      alreadyInSave = false;
        }
      }

                  
      /**
     * Set the PrimaryKey using ObjectKey.
     *
     * @param key id ObjectKey
     */
    public void setPrimaryKey(ObjectKey key)
        
    {
            setId(((NumberKey) key).intValue());
        }

    /**
     * Set the PrimaryKey using a String.
     *
     * @param key
     */
    public void setPrimaryKey(String key) 
    {
            setId(Integer.parseInt(key));
        }

  
    /**
     * returns an id that differentiates this object from others
     * of its class.
     */
    public ObjectKey getPrimaryKey()
    {
          return SimpleKey.keyFor(getId());
      }
 

    /**
     * Makes a copy of this object.
     * It creates a new object filling in the simple attributes.
       * It then fills all the association collections and sets the
     * related objects to isNew=true.
       */
      public VarietyInfo copy() throws TorqueException
    {
            return copy(true);
        }
  
          /**
     * Makes a copy of this object.
     * It creates a new object filling in the simple attributes.
     * If the parameter deepcopy is true, it then fills all the
     * association collections and sets the related objects to
     * isNew=true.
     *
     * @param deepcopy whether to copy the associated objects.
     */
    public VarietyInfo copy(boolean deepcopy) throws TorqueException
    {
        return copyInto(new VarietyInfo(), deepcopy);
    }
      
      /**
     * Fills the copyObj with the contents of this object.
     * The associated objects are also copied and treated as new objects.
     * @param copyObj the object to fill.
     */
    protected VarietyInfo copyInto(VarietyInfo copyObj) throws TorqueException
    {
        return copyInto(copyObj, true);
    }
      
    /**
     * Fills the copyObj with the contents of this object.
       * If deepcopy is true, The associated objects are also copied 
     * and treated as new objects.
       * @param copyObj the object to fill.
       * @param deepcopy whether the associated objects should be copied.
       */
      protected VarietyInfo copyInto(VarietyInfo copyObj, boolean deepcopy) throws TorqueException
      {
          copyObj.setId(id);
          copyObj.setFkVarietyId(fkVarietyId);
          copyObj.setEpisodeCount(episodeCount);
          copyObj.setTotalCount(totalCount);
          copyObj.setFixed(fixed);
          copyObj.setFirstlogo(firstlogo);
  
                            copyObj.setId( 0);
                                          
          if (deepcopy) 
        {
            }
          return copyObj;
    }

    /**
     * returns a peer instance associated with this om.  Since Peer classes
     * are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     */
    public VarietyInfoPeer getPeer()
    {
        return peer;
    }

    /**
     * Retrieves the TableMap object related to this Table data without
     * compiler warnings of using getPeer().getTableMap().
     *
     * @return The associated TableMap object.
     */
    public TableMap getTableMap() throws TorqueException
    {
        return VarietyInfoPeer.getTableMap();
    }


    public String toString()
    {
        StringBuffer str = new StringBuffer();
        str.append("VarietyInfo:\n");
        str.append("Id = ")
               .append(getId())
             .append("\n");
        str.append("FkVarietyId = ")
               .append(getFkVarietyId())
             .append("\n");
        str.append("EpisodeCount = ")
               .append(getEpisodeCount())
             .append("\n");
        str.append("TotalCount = ")
               .append(getTotalCount())
             .append("\n");
        str.append("Fixed = ")
               .append(getFixed())
             .append("\n");
        str.append("Firstlogo = ")
               .append(getFirstlogo())
             .append("\n");
        return(str.toString());
    }
}
